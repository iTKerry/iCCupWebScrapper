<UserControl 
    x:Class="iCCup.UI.View.Tab.GameProfileView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    mc:Ignorable="d" >
    <Grid>
        <StackPanel>
            <TextBlock Text="{Binding Nickname}"></TextBlock>
            <Button 
            VerticalAlignment="Stretch" Width="80"
            Command="{Binding GoBackCommand}" Style="{StaticResource MaterialDesignFlatButton}">Back</Button>
        </StackPanel>
    </Grid>
</UserControl>
